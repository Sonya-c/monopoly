class ServicioPublico extends Propiedad {
    /**
     * 
     * @param {Number}  id
     * @param {String}  nombre
     * @param {Image}   img 
     * @param {Number}  precio 
     * @param {Number}  renta
     */
    constructor(id, nombre, img, precio, renta) {
        super(id, nombre, img, precio, renta);
    }

    /**
     * 
     * @param {jugador} jugador
     * @param {Numero}  dados  
     */
    accion(jugador, dados) {

    }

}
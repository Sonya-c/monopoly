class Estacion extends Propiedad {

    /**
     * 
     * @param {Number} id 
     * @param {String} nombre 
     * @param {Image} img 
     * @param {Number} precio 
     */
    constructor(id, nombre, img, precio) {
        super(id, nombre, img, precio);
    }
}
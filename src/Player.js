class Player {
    /**
     * 
     * @param {String} nombre 
     * @param {Image} ficha 
     */
    constructor(nombre, ficha) {
        this.dinero = 0;
        this.nombre = nombre;
        this.ficha = ficha;
        this.linkPropiedad = null;
    }

    /**
     * Manejo de listas circules (?)
     * @param {Object} PTR 
     * @param {Propiedad} propiedad 
     */
    comprarPropiedad(PTR, propiedad) {

    }
}